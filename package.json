{
  "name": "docker-blogs",
  "version": "1.0.0",
  "private": true,
  "description": "Daily blog posts, published to GitHub Pages via Eleventy.",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
